[project]
name = "chatter-frontend"
version = "0.1.0"
description = "RAG knowledge Q&A Streamlit frontend"
readme = "../README.md"
requires-python = ">=3.12"
dependencies = [
    "streamlit~=1.52.1",
    "requests~=2.32.5",
    "python-dotenv~=1.0.0",
]

[tool.uv]
package = false
